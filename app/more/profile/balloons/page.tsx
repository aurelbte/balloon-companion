"use client";
import { ChevronLeft, ChevronRight, Plus } from "lucide-react";
import Link from "next/link";
import { useBalloonRegistry } from "../../../hooks/useBalloons";
import styles from "../../More.module.css";
export default function BalloonsPage() { const registry = useBalloonRegistry(); return <main className={styles.screen}><div className={styles.layout}><Link href="/more" className={styles.back}><ChevronLeft size={18} /> Plus</Link><header><p className={styles.eyebrow}>Mes ballons</p><h1 className={styles.title}>Mes ballons</h1><p className={styles.subtitle}>Les ballons utilisés dans Balloon Companion.</p></header>{registry.balloons.length === 0 && <p className={styles.documentEmpty}>Aucun ballon enregistré</p>}<div className={styles.balloonList}>{registry.balloons.map((balloon) => <Link className={styles.balloonCard} href={`/more/profile/balloons/${encodeURIComponent(balloon.id)}`} key={balloon.id}><span><strong>{balloon.registration}</strong><small>{balloon.manufacturer} {balloon.model}</small><small>{balloon.category}</small>{registry.activeBalloonId === balloon.id && <em>Ballon actif</em>}</span><ChevronRight size={19} /></Link>)}</div><Link className={styles.addBalloon} href="/more/profile/balloons/new"><Plus size={18} /> Ajouter un ballon</Link></div></main>; }
